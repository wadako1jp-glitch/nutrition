# 栄養計算アプリ — プロジェクト指示書

## 目的
日本食品標準成分表（八訂）をベースに、栄養計算を簡易的に行えるアプリを立ち上げ、安定稼働させる。

## ターゲット
- 一次ターゲット: 栄養管理課程の学生（教育用途で押す）
- 発注量算出など、管理栄養士・栄養学の学生の実務/学習フローを意識する

## 技術スタック（v1: Webアプリ / PWA として様子見。ネイティブは後日再検討）
- **方針転換（重要）**: 当初はReact Native + Expoでネイティブアプリを想定していたが、データのほとんどをDBで持たない構成のため、**まずはスマホ向けWebアプリ（PWA）で様子を見る**方針に変更。ストア登録・審査・年会費が不要で身軽に検証できる。ネイティブ化（Expo）は将来、必要になった時点で再検討する（下記ストレージ抽象がそのまま活きる）
- **フロント: React + Vite（Web）。PWA化**（manifest + service worker でホーム画面に追加・オフライン動作に対応）
  - 理由: 開発プロセスをAI駆動（Claude Code等）で進める前提のため、学習データが豊富なTS/React系を採用
  - 用紙モック（`.dc.html`）はHTML/CSSそのものなので、Web版のUI実装にほぼ流用できる
- **オフライン対応は最初から作り込む（要件確定）**: 使用場面は調理実習の教室だけでなく自宅なども含み、接続環境の強弱を確定できない。現時点では弱い方（オフライン）に合わせておく。Service Workerでアプリ本体・静的データ（成分表JSON等）をキャッシュし、電波なしでも起動・計算できるようにする
- **配布: URLを開くだけ。ストア無し**。Apple Developer/Google Play登録は当面不要（roadmap参照、先送りであって破棄ではない）
- **バックエンド: なし（サーバーレス、完全ローカル）**
- **データ層: DB不使用、インメモリ計算**
  - 成分表（八訂）はアプリ読み込み時にJSONを取得しメモリ上のMapにインデックスして保持。計算は全てメモリ上の純粋関数（`src/core/`）で完結させ、DBアクセスを発生させない
  - 一般的な献立の参照データとして、文科省・学校給食関連の公的な献立基準データを取り込む。これも成分表と同様、事前にJSON化して静的にバンドルする（実行時ネットワーク取得はしない＝オフライン・サーバーレスの前提を維持）。改訂に備え、成分表と同じくバージョン管理する（`data/school-meal-standards/<年度/版>/`）
  - 献立（レシピ）は端末内に保存・編集できる。一般的なレシピをインポートしてカスタム（足りない栄養素を補うために材料を足す/差し替える）し、エクスポートして共有できる
  - **保存はブラウザストレージ（IndexedDB。単純なキー・バリューはlocalStorageで十分な範囲のみ）** を採用。SQLite等の本格DBは不要。バックエンドは引き続きなし
  - **ストレージは `src/lib/storage/` に薄い抽象を置き、呼び出し側は実装（ブラウザストレージ／将来のMMKV等）を直接知らない**（`get(key)` / `set(key, value)` / `remove(key)` 程度のインターフェース）。Web→ネイティブに切り替える際もこの層だけ差し替えれば済む
  - 容量見積もり: 静的データ（八訂＋辞書＋給食レシピ数自治体分）でJSON 2〜3MB・メモリ5〜10MB、ユーザーデータは年間数百KB。DB不要の根拠（docs/decisions.md 参照）
  - インポート/エクスポート形式はアプリ独自JSON。Web版では`<a download>`によるファイル書き出し／Web Share APIでの共有を使う
  - パフォーマンスは実装レベル（Reactの再レンダリングからロジックを分離、成分表・献立データの検索をO(1)化）で担保する

## UI・入力体験の要件（最重要）

- **UIのベースは表（ワークシート）**。1行＝1材料で、列は「削除」「材料名」「使用量(g)」＋栄養素列（エネルギー・蛋白質・脂質・炭水化物・食物繊維・カルシウム・鉄・ビタミンA・ビタミンD・ビタミンB1・ビタミンB2・ビタミンC・食塩相当量）。最下行に小計行。
  - 材料名・使用量・削除列は横スクロール時も左に固定（sticky）、栄養素列は横スクロール。
  - デザインは無骨でよい（見た目より入力速度と用紙的な一覧性）。
- **行を打つそばから数値が埋まる**（「材料を全部入れてから計算」は不可）。ただし食品候補を選ぶまでその行は未確定として扱う（下記）。
- **食材入力・検索**: 材料名セルに入力すると、その行のすぐ下に候補一覧が表の1行として展開される（別ウィンドウやボトムシートにはしない。他の行は候補の分だけ下にずれる）。候補をクリック/タップすると確定し、候補行は閉じる。あいまい検索（表記ゆれ吸収）・食品番号直打ちに対応。
- **未確定行の扱い（重要）**: 材料名を入力しただけで候補をまだ選んでいない行は「未確定」。未確定行は栄養素が空欄のまま表示され、小計にも一切含めない。カード化した一覧のように未確定行を「入力済み」として見せることはしない。
- **献立名**: 自由入力させず「yyyymmdd_食事区分」（例: `20260924_昼食`）の無機質な形式。日付は作成日で固定表示、区分（朝食・昼食・夕食・間食）だけプルダウンで選ぶ（新規は作成時刻から推定）。旧版の自由入力の献立名は区分を選ぶまでそのまま残る。ロジックは `src/core/menuTitle.ts`。※この食事区分は献立名だけのもので、材料の料理タグ（主食・主菜…）とは別物。
- **行の削除**: 各行の左端に「×」ボタンを常設。押すと確認ポップアップ（対象の材料名を表示、「削除する」/「キャンセル」）が出て、確定した削除のみ行を消す。確認ポップアップの「次から確認せずに削除する」にチェックすると以降は確認なしで連続削除できる（設定は⚙「プロフィール・設定」で元に戻せる。献立そのものの削除は常に確認する）。スワイプ操作・長押しジェスチャーは採用しない（過去に試して操作性が悪く不採用になった）。
- **エクスポート（画像/スクショ向け表示）**: 入力用と同じ用紙形式の表（操作列なし）を、**横長・全画面・スクロール無し**で表示する。端末の表示領域を測って表全体が1画面に収まる縮尺に自動調整し、縦持ちなら中身を90°回転して横長で見せる（対応端末では全画面化・横向き固定も試みる）。操作ボタンは数秒で自動的に隠れ、スクリーンショット1枚に献立全体が写ることを狙う。LINE・Notepad等への転記・共有が目的。
  - **共有**: 「共有」ボタンで、表示内容を横長PNG（端末の向きに関係なく回転なし）にしてOS標準の共有シート（Web Share API、iOS・Androidとも画面下から出るもの）を開く。独自のLINE等のボタンは作らない。iOSは共有シートをタップ直後にしか開けないため、画像は表示と同時に先に生成しておく（`html-to-image`）。非対応ブラウザでは画像を保存する。
- **ワイド表示（変更仕様004）**: 表示幅が確保できるとき（`(min-width: 700px), (orientation: landscape) and (min-width: 600px)`。CSSの@mediaと `src/hooks/useMediaQuery.ts` の `WIDE_QUERY` は必ず揃える）、材料表を画面幅に詰めて全項目を横スクロール無しで表示する。編集はそのまま可能。ツールバーと材料追加欄は既定で隠し、画面タップで表示／非表示（数秒で自動的に隠れる。入力中の欄を含む場合は `:focus-within` で隠さない）。縦画面の表示項目は減らさない。画像用表示はそのまま残す（回転ロック利用者と提出用スクショのため）。
  - **厳守: レイアウト切替はCSSのみ。** 表のDOMは縦横共通で、JSで狭い版/広い版を出し分けたり input の親を変えたりしない（再マウントで入力値・フォーカス・IMEの変換中文字が消える）。JSが触るのはツールバー表示用の `chrome-shown` クラスだけ。
- **料理タグ（変更仕様002 A）**: 献立内の材料を「主食」「主菜」等にまとめる。階層のネストではなく**材料行への単一タグ付け**（`dishId`、null=未割当）。同じタグの行は淡色でハイライトし、並びはタグ順→登録順（未割当は末尾）、タグごとに小計行を出す。タグを1つも使っていない献立では「未割当 小計」は献立小計と同じになるため出さない。ロジックは `src/core/dishes.ts`。
  - **タグの管理UIは持たない**（画面が狭いため。こうしろうさんの判断で仕様002のタグ作成・削除UIを廃止）。材料追加欄と各材料行のプルダウンで直接選ぶ。選択肢はプリセット（主食・主菜・副菜・副菜2・汁物・デザート）＋「＋自由入力…」。タグは選んだ時点で作られ、どの材料にも使われなくなったら自動で消える。
- **利用者プロフィール（同 B）**: 年齢・性別・身体活動レベル（必須）、身長・体重（任意）を1件だけ端末に保存。献立一覧の⚙から編集。
- **合計・充足率（同 C）**: 献立一覧でチェックした献立を合算し、『日本人の食事摂取基準（2025年版）』と比較する。基準値は `src/data/dri/2025.ts`（版ごとに分離）、評価ロジックは `src/core/intake.ts`。脂質・炭水化物は%エネルギー（4/9/4 kcal/g）で目標範囲と比較、食塩相当量と脂質の上限側は超過を警告色。基準が七訂ベースで本アプリは八訂計算である旨の注記は常時表示（削除不可）。
- 将来: レコメンド機能（例: 納豆 → 納豆のタレ）
- iPhone必須の理由: 調理実習のたびにPCを持ち運ぶ負担を無くす（手軽さがコア）

## 計算ルール
- 丸めは小数第1位
- 小計は献立ごと。「行ごとに小数第1位で丸めてから合計」（手計算と同じ順序。合計してから丸めるのは不可）

### 入力と栄養計算の関係（確定仕様）
入力は **(食品名, 使用量)** の1組のみ。**使用量＝実際に料理で使う可食部の重さ**であり、これをそのまま栄養計算に使う。

- 「栄養計算重量」のような、使用量とは別に編集する変換後フィールドは**持たない**。
- 廃棄率換算（仕入れ量への換算）・調理による重量変化（生→めし等）・おろし物の歩留まり係数といった「使用量→計算用重量の自動変換」は、**現時点では実装しない**方針を確定した（こうしろうさんの判断。「廃棄率は今はいい」＝将来的に必要になれば別途検討するが、今のスコープには入れない）。
- 画面上・データ上は使用量の1本のみを持ち、それが確定した食品の可食部100gあたり成分値に対してそのまま比例計算される。

### アプリが出す数字の性質（重要）
このアプリが計算するのは**物理的な真値ではなく、入力した重量に基づく「計算上のPFC・栄養バランス」**。学生が手計算で行っていることと同じ性質のもので、目指すのは用紙と同じロジックで計算し切ることであり、実測の完全な再現ではない。
→ おろし物などの個別係数（例: おろし生姜 1g→0.24g）や廃棄率のような補正は、学生が実際の使用量を入力する時点である程度織り込まれる想定とし、アプリ側で自動算出・別フィールド管理はしない。

### データモデルへの影響
- 表記ゆれ辞書は名寄せ（検索ヒット用）のみを目的とし、「解決ルール」（廃棄率／変化率／参照食品コード）は**持たせない**。
- `data/cooking-weight-change/`（重量変化率表）の取り込みは**現時点では不要**（将来の検討事項としては残すが、実装対象外）。
- `src/core/nutrition.ts` の `purchaseWeight()`（廃棄率から仕入れ量を出す関数）は現状UIから使われていない未使用コード。将来この方針を覆さない限り削除候補。

## 出力・位置づけ
- 出力はオンライン（アプリ内表示）＋画像/スクショ向けレイアウトでの共有。PDF/Excel出力は当面なし
- 現時点では学校非公認の非公式アプリ。最悪は結果を紙の用紙に転記して提出する運用

## 重要な設計方針
- **成分表データはバージョン管理する前提**で設計する。改訂（八訂→将来の改訂版）が来ても
  コードを大きく変えずに切り替えられるよう、`data/mext-tables/<版>/` にデータを分離し、
  `src/data/` 側でどの版を使うか選択できる構造にする。
- **料理区分（スープ、おかず等）ごとの塩分パーセント可視化**は差別化ポイントなので、
  `src/features/salt-ratio/` として独立モジュールに切り出す。
- 市場規模は小さい前提。派手な機能より「痒いところに手が届くか」を優先し、
  開発コストとのバランスを常に意識する（過剰な作り込みをしない）。

## ディレクトリ構成
- `docs/` — 要件・意思決定ログ
- `data/mext-tables/` — 成分表データ（版ごとにサブディレクトリ）
- `data/school-meal-standards/` — 文科省・学校給食関連の公的な献立基準データ（参照用、版ごとにサブディレクトリ）
- `src/core/` — 栄養計算ロジック（Reactに依存しない純粋ロジック）
- `src/data/` — 成分表パーサ・ローダー
- `src/screens/` — 画面（Web版はページ/ルート）
- `src/components/` — UIコンポーネント
- `src/hooks/` — カスタムフック
- `src/lib/` — 共通ユーティリティ
- `src/lib/storage/` — 永続化の抽象層（実装はブラウザストレージ。差し替え可能にする）
- `public/` — PWA用manifest.json・アイコン・service worker
- `src/features/` — 差別化機能（塩分%可視化など）
- `tests/` — テスト
- `scripts/` — 発注量算出などのバッチ処理

## プロジェクトの狙い・課金モデル
- **一番の狙いは信用構築・実績づくり**（Note執筆と同じ路線）。収益は二の次
- **課金モデル: 基本無料 ＋ 300円買い切りで一部機能アンロック**
  - 無料: 食材からの栄養計算（成分表ベース）＋ 料理区分ごとの塩分%可視化
    → 競合が多い一般機能はここに寄せ、DL数・利用実績を最大化する
  - 有料（¥300買い切り）: 学校給食基準データを使った献立参照機能 ＋ 発注量算出機能
    → 一般的な栄養計算アプリにはない実務寄りの差別化機能。ここにマネタイズの実績も持たせる
  - サブスクは不採用（サーバーレスで運用コストがそもそも発生しない／学生向けで継続課金への抵抗感が高いため）
  - 広告は入れない（体験の質を落とし信用構築という目的と逆行するため）

## 未決定事項
- 配布ストア（App Store / Google Play）の具体的な提出タイミング
- 学校給食基準データの具体的な取得元資料の特定（文科省サイトのどの資料を使うか）
- 廃棄率・調理重量変化などの自動変換を将来的に復活させるかどうか（現時点では見送り。使用量をそのまま使う設計で運用してみて、必要性が出たら再検討）
